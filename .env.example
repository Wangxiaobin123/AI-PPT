# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Supported providers:
#   anthropic       — Anthropic Claude (default)
#   openai          — OpenAI GPT
#   deepseek        — DeepSeek
#   ollama          — Ollama (local)
#   together        — Together AI
#   groq            — Groq
#   moonshot        — Moonshot / Kimi
#   zhipu           — Zhipu / GLM
#   siliconflow     — SiliconFlow
#   openai_compatible — Any OpenAI-compatible API (requires LLM_BASE_URL)

LLM_PROVIDER=anthropic

# Provider-specific API keys (only the one matching LLM_PROVIDER is needed)
ANTHROPIC_API_KEY=sk-ant-xxx
OPENAI_API_KEY=sk-xxx
DEEPSEEK_API_KEY=sk-xxx

# Generic API key — used when the provider-specific key above is empty.
# Useful for together, groq, moonshot, zhipu, siliconflow, openai_compatible.
LLM_API_KEY=

# Model identifier (must match the provider)
# Examples:
#   anthropic:  claude-sonnet-4-20250514
#   openai:     gpt-4o
#   deepseek:   deepseek-chat, deepseek-reasoner
#   ollama:     llama3, qwen2, mistral
#   together:   meta-llama/Llama-3-70b-chat-hf
#   groq:       llama3-70b-8192
#   moonshot:   moonshot-v1-8k
#   zhipu:      glm-4
LLM_MODEL=claude-sonnet-4-20250514

# Custom base URL — required for openai_compatible, optional override for
# well-known providers (deepseek, ollama, together, groq, etc.)
LLM_BASE_URL=

# =============================================================================
# Server Configuration
# =============================================================================
HOST=0.0.0.0
PORT=8000
DEBUG=true

# =============================================================================
# Output Configuration
# =============================================================================
OUTPUT_DIR=./output
MAX_FILE_SIZE_MB=50

# =============================================================================
# Skills Configuration
# =============================================================================
SKILLS_PUBLIC_DIR=./src/skills/public
SKILLS_USER_DIR=./src/skills/user
